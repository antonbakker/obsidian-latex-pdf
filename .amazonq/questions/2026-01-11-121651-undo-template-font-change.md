# Question
Restore original template after switching fonts.

# Context
User removed powerline fonts and installed mono fonts; wants template reverted to original unmodified version. Repository currently shows only scripts/gh-release.js modified plus existing question files. Template file not obvious from git status.

# Outcome
Pending clarification of which template file/path should be restored; no changes applied yet.
